
[ENV_PATH]
Type = Regex
Regex = ENV_PATH.+?PATH=(?P<EnvPath>.*)

[FAIL_DELAY]
Type = Regex
Regex = FAIL_DELAY.+?(?P<FailDelay>\d+)

[PASS_MAX_DAYS]
Type = Regex
Regex = PASS_MAX_DAYS.+?(?P<PassMaxDays>\d+)

[LOGIN_RETRIES]
Type = Regex
Regex = LOGIN_RETRIES.+?(?P<LoginRetries>\d+)
